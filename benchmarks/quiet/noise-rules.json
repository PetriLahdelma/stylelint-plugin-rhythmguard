{
  "$comment": "Heuristic classification for the quiet benchmark. Anything matched here counts as a false positive. Per-repo labels in labels/<repo>.json override these. Hairlines (1px and smaller) are exempted by the rules themselves via allowHairlines, so they are no longer a benchmark allowance.",
  "noisePaths": [
    {
      "category": "noise:non-authored",
      "pattern": "(^|/)(vendor|third[-_]?party|node_modules|dist|build|generated|__snapshots__|fixtures?|tests?|__tests__|e2e|storybook-static|\\.storybook)(/|$)",
      "reason": "generated, vendored or test-only CSS is not authored design-system code"
    },
    {
      "category": "noise:minified",
      "pattern": "\\.min\\.css$",
      "reason": "minified bundles are build output"
    }
  ],
  "allowances": []
}
