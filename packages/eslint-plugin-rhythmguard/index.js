'use strict';

// eslint-plugin-rhythmguard is the ESLint companion of stylelint-plugin-rhythmguard
// under its own name, so a search for an ESLint Tailwind plugin finds it. The rules,
// options and messages are the same object; there is one implementation.
module.exports = require('stylelint-plugin-rhythmguard/eslint');
